<h1>Tags#index</h1>
<p>Find me in app/views/tags/index.html.erb</p>

<% @tags.each do |tag| %>
  <%= link_to users_path(tag_name: tag) do %>
    <span style="background-color: #e9e9e9; border-radius: 5px;"><%= tag %></span>
  <% end %>
<% end %>